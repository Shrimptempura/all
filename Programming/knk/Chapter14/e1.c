#include <stdio.h>
// The cube of x.
#define CUBE(x) ((x) * (x) * (x))
// The remainder when n is divided by 4.
#define DIV(n) ((n) % (4))
// 1 if the product of x and y is less than 100, 0 otherwise.
#define LESS_1_TO (((x) * (y)) < 100 ? 1 : 0)